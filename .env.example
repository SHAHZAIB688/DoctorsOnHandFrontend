# Backend REST + Socket.io base (socket uses same host without /api)
# VITE_API_URL=http://localhost:5000/api
#
# Optional: self-hosted PeerServer instead of PeerJS cloud
# VITE_PEER_HOST=
# VITE_PEER_PORT=443
# VITE_PEER_PATH=/
# VITE_PEER_SECURE=true
